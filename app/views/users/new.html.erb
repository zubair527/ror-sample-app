<% provide(title: 'Sign Up') %>
<h1>Sign Up</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |field| %>
        <%= field.label :name %>
        <%= field.text_field :name %>

        <%= field.label :email %>
        <%= field.email_field :email %>
        
        <%= field.label :password %>
        <%= field.password_field :password %>

        <%= field.label :password_confirmation, 'Confirmation' %>
        <%= field.password_field :password_confirmation %>

        <%= field.submit 'Create my account', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>

